{
  "name": "northstar-crm-challenge",
  "version": "0.0.0",
  "private": true,
  "description": "Source-identical baseline for the SOL versus Gaia CRM challenge",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "db:reset": "node scripts/db.mjs reset",
    "db:seed": "node scripts/db.mjs seed",
    "format": "prettier --check .",
    "format:write": "prettier --write .",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts tests/**/*.test.ts",
    "test:db": "node --test test/*.test.mjs",
    "test:fixtures": "node --test tests/**/*.test.mjs",
    "test:browser": "node scripts/run-browser-tests.mjs",
    "check": "node scripts/static-check.mjs && node scripts/verify-tests.mjs",
    "ci": "npm run format && npm run typecheck && npm run check && npm test && npm run test:db && npm run test:fixtures && npm run test:browser",
    "build": "vite build && tsc -p tsconfig.server.json",
    "dev": "tsx src/server/dev.ts",
    "start": "node dist/server/main.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "better-sqlite3": "^13.0.3",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "@vitejs/plugin-react": "^6.0.5",
    "prettier": "^3.9.6",
    "tsx": "^4.23.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.0"
  }
}
